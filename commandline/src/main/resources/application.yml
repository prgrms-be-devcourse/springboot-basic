---
spring:
  config:
    activate:
      on-profile: "dev"

voucher.path: "./VoucherList.csv"
logging:
  level: warn
---
spring:
  config:
    activate:
      on-profile: "default"

voucher.path: "./VoucherList.csv"