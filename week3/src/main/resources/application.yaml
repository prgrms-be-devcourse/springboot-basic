logging:
  level:
    root: warn

---
# jar 배포할때 dev or local 변경 배포 하기 위해 추가
spring:
  profiles:
    default: local

---
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost/order_mgmt
    username: root
    password: 1234