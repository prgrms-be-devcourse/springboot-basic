spring:
  profiles:
    active: local
  datasource:
    url: jdbc:mysql://localhost/order_mgmt
    username: root
    password: root1234!
  thymeleaf:
    prefix: "classpath:/views/"
server:
  servlet:
    context-path: /kdt

kdt:
  version: "v1.0"
