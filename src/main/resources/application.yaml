spring:
  profiles:
    active: jdbc
  datasource:
    url: jdbc:mysql://localhost:3306/springbasic
    username: root
    password: hjmaharu
  sql:
    init:
      mode: always
voucher:
  file: src/main/resources/vouchers.csv
