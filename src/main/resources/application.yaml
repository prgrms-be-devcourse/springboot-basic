spring:
  profiles:
    active: prod
---
spring:
  config:
    activate:
      on-profile: dev

file-path:
  blacklist: file:customer_blacklist.csv
---
spring:
  config:
    activate:
      on-profile: prod

file-path:
  voucher: src/voucherFile.csv
  blacklist: file:customer_blacklist.csv