<configuration>
    <property name="ROLLING_FILE_LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS}  %highlight(%-5level) %5X{PID} [%+12thread] %highlight(%-48logger{48}) : %msg%n" />
    <property name="CONSOLE_LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS}  %highlight(%-5level) %5X{PID} [%+12thread] %highlight(%-48logger{48}) : %msg%n" />

    <timestamp key="byDay" datePattern="yyyyMMdd" />

    <appender name="ROLLING_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/%d{yyyy-MM-dd}.log</fileNamePattern>
        </rollingPolicy>
        <encoder>
            <pattern>${ROLLING_FILE_LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
        </encoder>
    </appender>


    <!-- Define Inidivisual Log Level -->
    <logger name="org.programmers.kdt" level="debug" />

    <!-- Default Root Log Level -->
    <root level="info">
        <appender-ref ref="STDOUT" />
    </root>
</configuration>