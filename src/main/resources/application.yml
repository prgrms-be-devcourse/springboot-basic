spring:
  profiles:
    active: prod


---
spring:
  config:
    activate:
      on-profile: prod
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost/voucher_mgmt
    username: root
    password: root1234!
  mvc:
    throw-exception-if-no-handler-found: true

server:
  error:
    whitelabel:
      enabled: false
    include-exception: true

---
spring:
  config:
    activate:
      on-profile: local

voucher:
  file-path: src/main/resources/vouchers.csv