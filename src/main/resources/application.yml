spring:
  profiles:
    default: file
---
spring:
  config:
    activate:
      on-profile: dev
---
spring:
  config:
    activate:
      on-profile: file
csv:
  file:
    path: "src/main/resources/vouchers.csv"
  separator: ","
