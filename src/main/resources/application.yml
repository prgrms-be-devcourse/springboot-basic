spring:
  profiles:
    default: file
---
spring:
  config:
    activate:
      on-profile: dev
---
spring:
  config:
    activate:
      on-profile: file
csv:
  file:
    voucher:
      path: "src/main/resources/files/vouch1ers.csv"
    customer:
      blacklist_path: "src/main/resources/files/customer_blacklist.csv"
  separator: ","
