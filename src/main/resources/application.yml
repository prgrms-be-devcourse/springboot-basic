# 현재 default인 경우
spring:
  profiles:
    active: default
  datasource:
    hikari:
      driver-class-name: org.h2.Driver
      #      url: jdbc:h2:tcp://localhost/~/voucher
      username: sa
      password:
      jdbc-url: jdbc:h2:tcp://localhost/~/voucher

customerListFilePath: "src/main/resources/csv/customerList.csv"

---
# 현재 프로파일이 dev인 경우
spring:
  profiles:
    active: dev

voucherFilePath: "src/main/resources/csv/voucher.csv"
---

#현재 프로파일이 local인 경우
spring:
  profiles:
    active: local