# 현재 default인 경우
spring:
  profiles:
    active: default

blacklistFilePath: "src/main/resources/csv/blacklist.csv"

---
# 현재 프로파일이 dev인 경우
spring:
  profiles:
    active: dev

blacklistFilePath: "src/main/resources/csv/blacklist.csv"
---

#현재 프로파일이 local인 경우
spring:
  profiles:
    active: local

voucherFilePath: "src/main/resources/csv/voucher.csv"