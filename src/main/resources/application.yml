spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/management
    username: root
    password: qwer1234!
  profiles:
    active: jdbc
  mvc:
    hiddenmethod:
      filter:
        enabled: true