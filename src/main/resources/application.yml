spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3307/voucherProgram?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 1234
  sql:
    init:
      mode: always
    batch:
      jdbc:
        schema: classpath:schema.sql
  thymeleaf:
    prefix: classpath:templates/
    suffix: .html
    cache: false
  mvc:
    hidden-method:
      filter:
        enabled: true
