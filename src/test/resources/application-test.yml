spring:
  application:
    name: voucher-management
  datasource:
    username: test
    password: root1234!
  hikari:
    minimum-idle: 1
    maximum-pool-size: 5
    pool-name: MYSQL_DB
  sql:
    init:
      mode: always
csv:
  file-path: ${java.class.path}
  file-name:
    customer: customer.csv
    voucher: voucher.csv