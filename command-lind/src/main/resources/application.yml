spring:
  profiles:
    active: prod
    group:
      development:
        - local
      production:
        - prod

#외부주입 방식 찾기

---

spring:
  config:
    activate:
      on-profile: local

---

spring:
  config:
    activate:
      on-profile: prod